# Generated by circom + snarkjs. See circuits/README.md for regen instructions.
# Nothing in build/ is committed directly — the release artifacts (wasm, final
# zkey, verification_key.json) are attached to GitHub Releases per circuit
# version, and consumers (farewell-claimer) pull them from the release.
build/
*.ptau
